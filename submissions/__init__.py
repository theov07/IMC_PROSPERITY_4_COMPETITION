"""Submission entrypoints.

Only a minimal public wrapper is tracked here. Backtests now load member
configs directly from ``prosperity.config`` and no longer depend on a large
generated ``submissions/`` tree being present in the repo.
"""
