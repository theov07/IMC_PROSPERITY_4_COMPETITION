# Submissions

Submission entrypoints live here.

- `champion.py`: default baseline
- `leo.py`: Leo variant
- `theo.py`: Theo variant
- `pietro.py`: Pietro variant

`main.py` at the repository root imports `submissions.champion` so the default upload path stays simple.

